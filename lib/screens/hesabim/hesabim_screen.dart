import 'package:flutter/material.dart';

class HesabimScreen extends StatelessWidget {
  static String routeName = "/hesabim";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(""),
      ),
    );
  }
}
